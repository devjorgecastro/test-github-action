name: Test Action
on:
  push:
    branches:
     - '.*feature/.*'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run My Action
        run: echo "Test Success!"
